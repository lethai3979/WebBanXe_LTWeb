@model LTWeb_CodeFirst.Models.Company

@{
    ViewData["Title"] = "Details";
}

<h1>Thông tin hãng xe</h1>

<div>
    <h4>Chi tiết</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            Hãng xe
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class="col-sm-2">
            Loại xe
        </dt>
        <dd class="col-sm-10">
            @foreach (var item in @ViewBag.companyCarTypes)
            {
                <div>@item</div>
            }
        </dd>
        <dt class = "col-sm-2">
            Ẩn
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.IsDeleted)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Chỉnh sửa</a> |
    <a asp-action="Index">Trở về trang hãng xe</a>
</div>
